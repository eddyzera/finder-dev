import React from 'react'
import { HeaderContainer, HeaderTitle } from './styles'

export const Header: React.FunctionComponent = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Finder Dev</HeaderTitle>
      <p>notification</p>
    </HeaderContainer>
  )
}